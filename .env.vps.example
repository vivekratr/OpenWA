# Only vars you must set. Compose forces postgres + redis + queue.
# cp .env.vps.example .env

DATABASE_PASSWORD=change-me-to-a-strong-password
CORS_ORIGINS=https://your-app.vercel.app

# optional
# API_PORT=2785
